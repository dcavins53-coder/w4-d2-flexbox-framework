# w4-d2-flexbox-framework

```
###
const someNums = [10, 15, 6, 14, 111, 100];
console.log(someNums);

//* let i = 0
//* i < 6
//*i++

//traditional for loop
for (let i = 0; i < 6; i++) {
  console.log(someNums[i]);
}

//arrays js & loops
const myArray = new Array();

//map returns an array) and forEach doesn't return an array 
const newArray = someNums.map(function (item) {
     console.log (item);
     return "Hello" + item


});

```